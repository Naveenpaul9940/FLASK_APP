[build]
publish = "static"
command = "pip install 3.8 && pip install -r requirements.txt && gunicorn app:app"

[[redirects]]
# Redirect all routes to your Flask server
from = "/*"
to = "/"
status = 200
